{
  "name": "car-dealer",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run start:dev -w apps/backend\" \"npm run dev -w apps/frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
